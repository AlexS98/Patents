@using Patents.Models
@model IEnumerable<Register>
@{
    ViewBag.Title = "RegistersTable";
}
<style>
    #cartTable td {
        vertical-align: middle
    }
</style>
<h2 class="text-center">Registers</h2>
<hr />
<table id="cartTable" class="table table-striped">
    <thead>
        <tr>
            <th class="text-center">InventorId</th>
            <th class="text-center">Name</th>
            <th class="text-center">Email</th>
            <th class="text-center">Role</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null)
        {
            foreach (var i in Model)
            {
                <tr>
                    <td class="text-center">@i.RegisterId</td>
                    <td class="text-center">@i.Name</td>
                    <td class="text-center">@i.Email</td>
                    <td class="text-center">@i.Role.UserRole</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td class="text-center">No data!</td>
                <td class="text-center">No data!</td>
                <td class="text-center">No data!</td>
                <td class="text-center">No data!</td>
                <td class="text-center">No data!</td>
            </tr>
        }
    </tbody>
</table>
<hr />

@Html.Action("SearchingForm", "Registers")